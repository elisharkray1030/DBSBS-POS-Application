# 04 — Sale screen

**What to build:** The main working screen is rebuilt with the modern widgets: the scrollable item list (ID, name, price, remaining count, status), the current sale panel, the quantity controls, the running total in a larger prominent font, a visually prominent settle button, the running summary (takings + sale count), and the sold-out toggle. Every action — add to sale, set quantity, remove, new sale, settle, toggle sold-out, open the sales list, record a cash adjustment, go to end-of-day — behaves exactly as before.

**Blocked by:** 02

**Status:** done

- [ ] The item list shows all catalog items with ID, name, price, remaining count, and status; sold-out items are dimmed and blocked from new sales
- [ ] Adding items builds the current sale, shows the running total prominently, and updates the running summary (takings + sale count)
- [ ] Quantity set/remove, new sale, sold-out toggle, sales list, cash adjustment, and end-of-day navigation all work as before
- [ ] The settle button and the current sale total are visually prominent and easy to find during busy periods
- [ ] The screen uses the modern widgets and stays consistent with the app's color theme
## Comments

> *This was generated by AI during triage.*

Implemented and committed in `a515594` (CustomTkinter UI migration). All acceptance criteria are covered and the facade suite passes unchanged. Closing as done.

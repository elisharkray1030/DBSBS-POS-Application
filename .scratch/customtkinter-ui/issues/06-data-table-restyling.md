# 06 — Data table restyling

**What to build:** The six retained data tables (item list, current sale panel, per-item sold counts, voids list, cash adjustments, recorded-sales list) are restyled to match the app's modern color scheme — themed headers, accent-colored selection highlight, adequate row height and spacing, and readable typography. The styling is shared across the tables so they stay consistent, and it adapts when the app switches between light and dark appearance. Sold-out items remain visually distinct (dimmed text) in both modes.

**Blocked by:** 03, 04, 05

**Status:** done

- [ ] All six tables use the app's color theme for headers, selection highlight, and text
- [ ] Row height and spacing make the tables easy to scan and select
- [ ] Sold-out items remain visually distinct in the item list in both light and dark appearance
- [ ] Table colors adapt correctly when the system appearance changes between light and dark
- [ ] The styling is shared in one place rather than duplicated per table
## Comments

> *This was generated by AI during triage.*

Implemented and committed in `a515594` (CustomTkinter UI migration). All acceptance criteria are covered and the facade suite passes unchanged. Closing as done.

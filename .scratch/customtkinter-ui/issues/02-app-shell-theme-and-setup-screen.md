# 02 — App shell, theme, and setup screen

**What to build:** The application root window becomes a modern themed shell: appearance follows the Windows light/dark setting, a default color theme is set, and high-DPI displays render sharp widgets with no manual scaling code. The first screen the cashier sees — the setup screen that loads the Stock sheet CSV, enters the float, and names the device — is rebuilt with the modern widgets while keeping the same fields, layout flow, and behavior. Starting the day still validates and advances to the sale screen as before.

**Blocked by:** None — can start immediately

**Status:** done

- [ ] The app launches to a modern-looking root window; appearance follows the system light/dark setting
- [ ] The setup screen renders with modern buttons, entry fields, and labels and the same fields (Stock sheet CSV, float, device name)
- [ ] Loading a valid Stock sheet CSV shows the loaded-item count; a bad file shows the error inline without leaving the screen
- [ ] Starting the day validates the float and device name and advances to the sale screen, exactly as before
- [ ] High-DPI displays render sharp, appropriately sized widgets with no manual scaling code
## Comments

> *This was generated by AI during triage.*

Implemented and committed in `a515594` (CustomTkinter UI migration). All acceptance criteria are covered and the facade suite passes unchanged. Closing as done.

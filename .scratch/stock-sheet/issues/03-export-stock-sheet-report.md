# 03 — Export the completed Stock sheet report

**What to build:** The end-of-day export writes a third file — the device's completed Stock sheet report, named after the device (e.g. `stocks-Till A.csv`) — alongside the existing `sales.csv` and `items.csv`. The report has the same six-column header and the same rows in the same order as the loaded master file; `ItemID`, `ItemName`, `Price`, and `Inventory` pass through unchanged, and only `Sales` and `Revenue` are filled: `Sales` is the item's final-state units sold on this device (excluding voids), `Revenue` is the actually-recorded settled value for the item (reflecting corrections). Items with no sales are written as `0`/`0`, a blank `Inventory` stays blank, and the loaded master file is never modified. The export surfaces all three files to the user.

**Blocked by:** 01 — Load the organizer's Stock sheet as the catalog.

**Status:** done

- [ ] The end-of-day export produces three files: the existing sales and items exports plus the device-named Stock sheet report
- [ ] The report's header and column order match the master file (`ItemID, ItemName, Price, Inventory, Sales, Revenue`)
- [ ] The report has one row per catalog item, in the master file's row order, with `ItemID`, `ItemName`, `Price`, and `Inventory` unchanged
- [ ] `Sales` equals the item's final-state, non-void units sold on this device
- [ ] `Revenue` equals the actually-recorded settled value for the item, with corrections in their final state
- [ ] Voided sales are excluded from both `Sales` and `Revenue`
- [ ] Items with no sales are written as `0`/`0`; blank `Inventory` cells stay blank
- [ ] The loaded master file is not modified by the export

## Comments

> *This was generated by AI during triage.*

Implemented and committed in `9f181db`. All acceptance criteria are covered; the full suite (89 tests) passes. Closing as done.
